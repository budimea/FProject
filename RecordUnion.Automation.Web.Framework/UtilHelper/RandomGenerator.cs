using System;

namespace RecordUnion.Automation.Web.Framework.UtilHelper
{
    public class RandomGenerator
    {
        public string RandomDigits(int length)
        {
            var random = new Random();
            int number;
            /*for (int i = 0; i < length; i++)
                s = String.Concat(s, random.Next(10));*/
            return "to be done";
        }
    }
}